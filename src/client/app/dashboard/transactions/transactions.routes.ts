import { Route } from '@angular/router';

import { TransactionsComponent } from './index';

export const TransactionsRoutes: Route[] = [
	{
		path: 'transactions',
		component: TransactionsComponent,
	}
];
