import { Route } from '@angular/router';

import { ExpiredMedicinesComponent } from './index';

export const ExpiredMedicinesRoutes: Route[] = [
	{
		path: 'expired-medicines',
		component: ExpiredMedicinesComponent,
	}
];
